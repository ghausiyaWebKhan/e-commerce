import FilterInput from "./FilterInput";

const Filter = ({price,quantity}) => {
  return (
    <>
        <FilterInput price={price} quantity={quantity} />
       
    
    </>
  );
};

export default Filter;
